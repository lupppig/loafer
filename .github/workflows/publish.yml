name: Publish to PyPI

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build and publish (e.g. v0.3.1)"
        required: true
        type: string

jobs:
  publish:
    name: Build & Publish
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment:
      name: pypi
      url: https://pypi.org/project/loafer-etl/
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          # On manual runs, build the requested tag rather than the default
          # branch so a re-publish targets the exact released commit.
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: "0.11.16"

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Run tests
        run: uv run pytest tests/unit/ -v

      - name: Resolve release version
        id: version
        run: |
          set -euo pipefail
          ref="${{ github.event.release.tag_name || github.event.inputs.tag }}"
          version="${ref#v}"
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "Resolved version: ${version}"

      # Pin the version explicitly. hatch-vcs derives the version from git, but
      # the test step above writes caches into the tree, marking it dirty and
      # producing a local version like 0.3.2.dev0+g<sha>.dYYYYMMDD that PyPI
      # rejects (400, local versions not allowed). Pinning from the release tag
      # — the same approach docker.yml uses — guarantees a clean release build.
      # Gate: build the wheel and run a real pipeline against it inside a clean
      # python:3.11-slim container (no dev extras, no repo .venv). A wheel that
      # can't run in a clean room — e.g. an undeclared runtime dep like the
      # missing `click` that made 0.3.0 crash on every command — must never
      # reach PyPI. smoke_wheel.sh leaves the tested wheel in dist/.
      - name: Smoke-test the wheel before publishing
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: bash scripts/smoke_wheel.sh

      # Build the sdist too (smoke_wheel.sh built only the wheel into dist/).
      # The wheel already in dist/ is the exact artifact the smoke test passed.
      - name: Build sdist
        env:
          SETUPTOOLS_SCM_PRETEND_VERSION_FOR_LOAFER_ETL: ${{ steps.version.outputs.version }}
          SETUPTOOLS_SCM_PRETEND_VERSION: ${{ steps.version.outputs.version }}
        run: uv build --sdist

      - name: Publish to PyPI
        run: uv publish --trusted-publishing always
