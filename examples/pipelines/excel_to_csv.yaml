name: Excel Inventory → CSV
source:
  type: excel
  path: ../data/inventory_data.xlsx
  sheet: Inventory

target:
  type: csv
  path: ../output/inventory_flat.csv

transform:
  type: custom
  path: ../transforms/excel_transform.py

mode: etl
chunk_size: 5000
