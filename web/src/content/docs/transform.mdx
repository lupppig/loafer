# Transform Modes
Last updated: March 2025  ·  6 min read  ·  [Edit on GitHub ↗](https://github.com/lupppig/loafer/edit/main/web/src/content/docs/transform.mdx)

Loafer gives you three ways to transform your data. You can choose the mode that best fits your technical proficiency and the complexity of the transformation.

> **Auto-detection:** The `type` field is optional. Loafer infers the transform mode from the fields present: `instruction` → AI, `path` → Custom Python, `query` → SQL. You can also pass a plain string as `transform` for quick AI mode.

## Overview

| Mode | When to use | LLM required | Retry support |
|---|---|---|---|
| **AI** | Quick, messy cleanups, complex logical routing. | Yes | Automatic schema-checking |
| **SQL** | Established analytics queries, aggregations. | No | No (Syntax errors fail fast) |
| **Python** | Heavy custom logic, external API lookups. | No | Extensively testable |

## AI mode

Supply basic natural language instructions, and your configured LLM (Gemini, Claude, or OpenAI) generates raw Python code to handle the transformation locally.

### Supported Providers

Loafer is provider-agnostic. You can configure any of the following in your `pipeline.yaml`:

- **Google Gemini** (default: `gemini-3.6-flash`)
- **OpenAI** (default: `gpt-5.6-terra`)
- **Anthropic Claude** (default: `claude-sonnet-5`)
- **Qwen** (default: `qwen3.7-plus`)

### How it works

1. **Schema Observation:** Loafer observes incoming records and extract column mappings.
2. **Privacy-First Prompting:** The schema and your instructions are sent to the LLM. **Raw row data is never sent to the LLM.**
3. **Local Execution:** The LLM returns a Python function which Loafer evaluates safely inside your local runtime.

> [!IMPORTANT]
> **Data Privacy:** Your production data stays 100% in your environment. The LLM only sees the "shape" of your data to write the logic.

### Writing good instructions

Good instructions yield performant code.

*   **Specify data types explicitly:** Instead of "fix the amounts", write "convert 'amount_kobo' to 'amount' divided by 100 as float."
*   **Handle nulls defensibly:** State clearly, "If 'address' is missing, set to 'Unknown'."
*   **Drop unwanted data boldly:** Tell it explicitly to prune out rows that don't match criteria.

### Understanding retries

If the generated AI function throws a runtime exception, the LLM catches the traceback error from Python and automatically issues a refinement correction attempt.

## SQL mode

Write transformations via standard ANSI SELECT queries. 

### The `{{source}}` placeholder

Because Loafer runs queries locally using sqlglot, the incoming stream is represented transparently under the `{{source}}` keyword matrix.

```yaml
transform:
  query: >
    SELECT
      id,
      amount / 100.0 AS amount_naira,
      UPPER(name)    AS name
    FROM {{source}}
    WHERE email IS NOT NULL
```

## Custom Python mode

If your transformation logic spans hundreds of lines or involves complex API integrations not fit for an LLM—write it yourself.

```yaml
transform:
  path: ./scripts/complex_logic.py
```

Inside your `.py` file you must export a `transform(data: list[dict]) -> list[dict]` handler.

## Sandboxing

Both AI-generated and custom transform code run in an **isolated, resource-limited subprocess** — not in the main Loafer process. On Linux/macOS the worker is capped by CPU and memory rlimits and a hard wall-clock kill, so a runaway or hostile transform becomes a terminated worker with a clear error instead of hanging the pipeline or reaching your database credentials. Code runs with a restricted set of builtins (no `open`, `eval`, `exec`, `__import__`) and only `re`, `json`, `datetime`, `math`, `decimal`, `uuid`, and `itertools` available.

Tune the limits with an optional `sandbox` block (defaults shown):

```yaml
sandbox:
  timeout: 60        # seconds before the worker is killed
  max_memory_mb: 512 # address-space cap for the worker
```

> [!NOTE]
> On Windows (no `resource` module) the sandbox falls back to an in-process thread with a best-effort timeout only — resource limits are not enforced, and a one-time warning is printed. Run Loafer on Linux (including the Docker image) for full isolation. Because each run spawns a fresh worker, pipelines pay a small per-transform startup cost.

<div className="flex w-full gap-4 mt-12">
  <a href="/docs/targets" className="flex-1 p-4 border border-border-default rounded-md bg-bg-surface hover:bg-bg-overlay transition-colors no-underline text-right">
    <div className="text-xs text-text-muted mb-1 font-medium select-none uppercase tracking-wide">Previous</div>
    <div className="text-[13px] font-semibold text-text-primary">&larr; Target Connectors</div>
  </a>
  <a href="/docs/cli" className="flex-1 p-4 border border-border-default rounded-md bg-bg-surface hover:bg-bg-overlay transition-colors no-underline">
    <div className="text-xs text-text-muted mb-1 font-medium select-none uppercase tracking-wide">Next</div>
    <div className="text-[13px] font-semibold text-text-primary">CLI Reference &rarr;</div>
  </a>
</div>
